# Sistema de Reservaciones de Hotel

Practica de encapsulamiento realizada en Java.

El proyecto utiliza atributos privados, constructores, metodos getters y setters.
Para ejecutarlo se debe abrir la carpeta del proyecto en Apache NetBeans y ejecutar la clase `Principal`.
